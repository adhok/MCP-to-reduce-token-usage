{
  "name": "token-saver-mcp",
  "version": "0.1.0",
  "type": "module",
  "description": "A minimal MCP server for token-saving utilities.",
  "files": [
    "dist",
    "README.md"
  ],
  "bin": {
    "token-saver-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "benchmark:compression": "npm run build && node scripts/benchmark-compression.mjs",
    "test:unit": "npm run build && node --test test/*.test.mjs",
    "test": "npm run test:unit && node scripts/test-validation.mjs && node scripts/test-cache.mjs && node scripts/test-token-budget.mjs && node scripts/test-run-and-summarize.mjs && node scripts/test-read-relevant.mjs && node scripts/test-code-search.mjs && node scripts/test-ast-parser.mjs && node scripts/test-installer.mjs && node scripts/test-session-stats.mjs",
    "start": "node dist/server.js",
    "verify:release": "npm test && npm pack --dry-run",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^13.0.1",
    "tree-sitter": "0.21.1",
    "tree-sitter-javascript": "0.23.1",
    "tree-sitter-python": "0.21.0",
    "tree-sitter-typescript": "0.23.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
